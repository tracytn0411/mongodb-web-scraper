{
  "name": "mongodb-web-scraper",
  "version": "1.0.0",
  "description": "web app that lets users view and leave comments on latest dev news.",
  "main": "server.js",
  "engines": {
    "node": "12.x"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"nodemon server.js\" \"cd client && npm run start\"",
    "build": "cd client && npm run build",
    "start": "node server.js",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/tracytn0411/mongodb-web-scraper.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/tracytn0411/mongodb-web-scraper/issues"
  },
  "homepage": "https://hot-news-scraper.herokuapp.com/",
  "dependencies": {
    "axios": "^0.19.0",
    "body-parser": "^1.19.0",
    "cheerio": "^1.0.0-rc.3",
    "colors": "^1.4.0",
    "concurrently": "^5.0.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "ejs": "^2.7.1",
    "express": "^4.17.1",
    "method-override": "^3.0.0",
    "mongoose": "^5.7.7",
    "request": "^2.88.0",
    "socket.io": "^2.4.0"
  }
}
